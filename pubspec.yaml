name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  image_picker: ^0.8.7+5
  qr_code_scanner: ^1.0.0
  
  # Agregamos barcode_scan2 para el escaneo de códigos de barras
  barcode_scan2: ^4.1.6

  # Agregamos MongoDB Realm para base de datos
  flutter_mongodb_realm: ^2.3.0
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Assets utilizados en la aplicación
  assets:
    - assets/LOGOBUKZ1.png
